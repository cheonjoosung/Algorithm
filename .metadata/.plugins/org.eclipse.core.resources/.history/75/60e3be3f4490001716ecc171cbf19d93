package codeground;

import java.util.Scanner;

public class Sol35 {
	static int Answer;

	public static void main(String args[]) throws Exception	{
		Scanner sc = new Scanner(System.in);

		int T = sc.nextInt();
		for(int test_case = 0; test_case < T; test_case++) {

			Answer = 0;

			int n = sc.nextInt();
			int [] tree = new int[n];
			int [] laser = new int[n];

			for(int i=0; i<n ; i++) {
				tree[i] = sc.nextInt();
				tree[i] += (n-1);
			}

			for(int i=0; i<n ; i++)
				laser[i] = sc.nextInt();

			int minLaser = Integer.MAX_VALUE;

			for(int j=0; j<n ; j++) {
				if(tree[j] - laser[j] > 0) {
					minLaser = Math.min(minLaser, laser[j]);
				}
			}

			for(int j=0; j<n ; j++) {
				if(tree[j] - minLaser + (n-1) > 0) {
					int temp = tree[j] - minLaser;
					Answer += temp;
				}
				//System.out.print(j + " " + temp + " ");

			}

			System.out.println("Case #"+(test_case+1));
			System.out.println(Answer);
		}
	}


}
